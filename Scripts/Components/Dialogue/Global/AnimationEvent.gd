extends DialogueEvents

class_name AnimationEvent

@export var animationStr:String
@export var flipSprite:bool
@export var newPos:Vector2 #Check if pos needs to be animated too
@export var animationTime:float #Set timer variable if animation loops
